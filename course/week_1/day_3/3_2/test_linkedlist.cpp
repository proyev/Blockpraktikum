#include <iostream>
#include "linkedlist.h"

int main() {
    int length;
    std::cout << "Enter the linked list length: " << std::flush;
    std::cin >> length;

//    for (int i = 0; i < length; i++){
//        elem = i * 5;
//    }

    return 0;
}
